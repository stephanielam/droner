<footer class="footer sticky-footer">
   <small>
   <% if current_client %>
   Signed in as <%= "Admin:" if current_client.admin %> <%= current_client.username %> -
   <% if current_client.admin %>
   <%= link_to "Edit Profile", edit_admin_client_path(current_client)%>
   <% else %>
   <%= link_to "Edit Profile", edit_client_path(current_client)%>
   <% end %>
   | <%= link_to "Log out", session_path("current"), method: :delete %>
   <% else %>
   Username: admin, Password: 123
   <% end %>
   </small>
</footer>

